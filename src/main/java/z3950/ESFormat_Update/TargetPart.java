/*
 * $Source$
 * $Date$
 * $Revision$
 *
 * Copyright (C) 1998, Hoylen Sue.  All Rights Reserved.
 * <h.sue@ieee.org>
 *
 * This program is distributed in the hope that it will be useful,
 * but WITHOUT ANY WARRANTY; without even the implied warranty of
 * MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  Refer to
 * the supplied license for more details.
 *
 * Generated by Zebulun ASN1tojava: 1998-09-08 03:15:27 UTC
 */

//----------------------------------------------------------------

package z3950.ESFormat_Update;
import asn1.*;
import z3950.v3.DiagRec;
import z3950.v3.InternationalString;

//================================================================
/**
 * Class for representing a <code>TargetPart</code> from <code>ESFormat-Update</code>
 *
 * <pre>
 * TargetPart ::=
 * SEQUENCE {
 *   updateStatus [1] IMPLICIT INTEGER
 *   globalDiagnostics [2] IMPLICIT SEQUENCE OF DiagRec OPTIONAL
 *   taskPackageRecords [3] IMPLICIT SEQUENCE OF TaskPackageRecordStructure
 * }
 * </pre>
 *
 * @version	$Release$ $Date$
 */

//----------------------------------------------------------------

public final class TargetPart extends ASN1Any
{

  public final static String VERSION = "Copyright (C) Hoylen Sue, 1998. 199809080315Z";

//----------------------------------------------------------------
/**
 * Default constructor for a TargetPart.
 */

public
TargetPart()
{
}

//----------------------------------------------------------------
/**
 * Constructor for a TargetPart from a BER encoding.
 * <p>
 *
 * @param ber the BER encoding.
 * @param check_tag will check tag if true, use false
 *         if the BER has been implicitly tagged. You should
 *         usually be passing true.
 * @exception	ASN1Exception if the BER encoding is bad.
 */

public
TargetPart(BEREncoding ber, boolean check_tag)
       throws ASN1Exception
{
  super(ber, check_tag);
}

//----------------------------------------------------------------
/**
 * Initializing object from a BER encoding.
 * This method is for internal use only. You should use
 * the constructor that takes a BEREncoding.
 *
 * @param ber the BER to decode.
 * @param check_tag if the tag should be checked.
 * @exception ASN1Exception if the BER encoding is bad.
 */

public void
ber_decode(BEREncoding ber, boolean check_tag)
       throws ASN1Exception
{
  // TargetPart should be encoded by a constructed BER

  BERConstructed ber_cons;
  try {
    ber_cons = (BERConstructed) ber;
  } catch (ClassCastException e) {
    throw new ASN1EncodingException
      ("Zebulun TargetPart: bad BER form\n");
  }

  // Prepare to decode the components

  int num_parts = ber_cons.number_components();
  int part = 0;
  BEREncoding p;

  // Decoding: updateStatus [1] IMPLICIT INTEGER

  if (num_parts <= part) {
    // End of record, but still more elements to get
    throw new ASN1Exception("Zebulun TargetPart: incomplete");
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() != 1 ||
      p.tag_type_get() != BEREncoding.CONTEXT_SPECIFIC_TAG)
    throw new ASN1EncodingException
      ("Zebulun TargetPart: bad tag in s_updateStatus\n");

  s_updateStatus = new ASN1Integer(p, false);
  part++;

  // Decoding: globalDiagnostics [2] IMPLICIT SEQUENCE OF DiagRec OPTIONAL

  if (num_parts <= part) {
    // End of record, but still more elements to get
    throw new ASN1Exception("Zebulun TargetPart: incomplete");
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() == 2 &&
      p.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    try {
      BERConstructed cons = (BERConstructed) p;
      int parts = cons.number_components();
      s_globalDiagnostics = new DiagRec[parts];
      int n;
      for (n = 0; n < parts; n++) {
        s_globalDiagnostics[n] = new DiagRec(cons.elementAt(n), true);
      }
    } catch (ClassCastException e) {
      throw new ASN1EncodingException("Bad BER");
    }
    part++;
  }

  // Decoding: taskPackageRecords [3] IMPLICIT SEQUENCE OF TaskPackageRecordStructure

  if (num_parts <= part) {
    // End of record, but still more elements to get
    throw new ASN1Exception("Zebulun TargetPart: incomplete");
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() != 3 ||
      p.tag_type_get() != BEREncoding.CONTEXT_SPECIFIC_TAG)
    throw new ASN1EncodingException
      ("Zebulun TargetPart: bad tag in s_taskPackageRecords\n");

  try {
    BERConstructed cons = (BERConstructed) p;
    int parts = cons.number_components();
    s_taskPackageRecords = new TaskPackageRecordStructure[parts];
    int n;
    for (n = 0; n < parts; n++) {
      s_taskPackageRecords[n] = new TaskPackageRecordStructure(cons.elementAt(n), true);
    }
  } catch (ClassCastException e) {
    throw new ASN1EncodingException("Bad BER");
  }
  part++;

  // Should not be any more parts

  if (part < num_parts) {
    throw new ASN1Exception("Zebulun TargetPart: bad BER: extra data " + part + "/" + num_parts + " processed");
  }
}

//----------------------------------------------------------------
/**
 * Returns a BER encoding of the TargetPart.
 *
 * @exception	ASN1Exception Invalid or cannot be encoded.
 * @return	The BER encoding.
 */

public BEREncoding
ber_encode()
       throws ASN1Exception
{
  return ber_encode(BEREncoding.UNIVERSAL_TAG, ASN1Sequence.TAG);
}

//----------------------------------------------------------------
/**
 * Returns a BER encoding of TargetPart, implicitly tagged.
 *
 * @param tag_type	The type of the implicit tag.
 * @param tag	The implicit tag.
 * @return	The BER encoding of the object.
 * @exception	ASN1Exception When invalid or cannot be encoded.
 * @see asn1.BEREncoding#UNIVERSAL_TAG
 * @see asn1.BEREncoding#APPLICATION_TAG
 * @see asn1.BEREncoding#CONTEXT_SPECIFIC_TAG
 * @see asn1.BEREncoding#PRIVATE_TAG
 */

public BEREncoding
ber_encode(int tag_type, int tag)
       throws ASN1Exception
{
  // Calculate the number of fields in the encoding

  int num_fields = 2; // number of mandatories
  if (s_globalDiagnostics != null)
    num_fields++;

  // Encode it

  BEREncoding fields[] = new BEREncoding[num_fields];
  int x = 0;
  BEREncoding f2[];
  int p;

  // Encoding s_updateStatus: INTEGER 

  fields[x++] = s_updateStatus.ber_encode(BEREncoding.CONTEXT_SPECIFIC_TAG, 1);

  // Encoding s_globalDiagnostics: SEQUENCE OF OPTIONAL

  if (s_globalDiagnostics != null) {
    f2 = new BEREncoding[s_globalDiagnostics.length];

    for (p = 0; p < s_globalDiagnostics.length; p++) {
      f2[p] = s_globalDiagnostics[p].ber_encode();
    }

    fields[x++] = new BERConstructed(BEREncoding.CONTEXT_SPECIFIC_TAG, 2, f2);
  }

  // Encoding s_taskPackageRecords: SEQUENCE OF 

    f2 = new BEREncoding[s_taskPackageRecords.length];

    for (p = 0; p < s_taskPackageRecords.length; p++) {
      f2[p] = s_taskPackageRecords[p].ber_encode();
    }

    fields[x++] = new BERConstructed(BEREncoding.CONTEXT_SPECIFIC_TAG, 3, f2);

  return new BERConstructed(tag_type, tag, fields);
}

//----------------------------------------------------------------
/**
 * Returns a new String object containing a text representing
 * of the TargetPart. 
 */

public String
toString()
{
  int p;
  StringBuffer str = new StringBuffer("{");
  int outputted = 0;

  str.append("updateStatus ");
  str.append(s_updateStatus);
  outputted++;

  if (s_globalDiagnostics != null) {
    if (0 < outputted)
    str.append(", ");
    str.append("globalDiagnostics ");
    str.append("{");
    for (p = 0; p < s_globalDiagnostics.length; p++) {
      if (p != 0)
        str.append(", ");
      str.append(s_globalDiagnostics[p]);
    }
    str.append("}");
    outputted++;
  }

  if (0 < outputted)
    str.append(", ");
  str.append("taskPackageRecords ");
  str.append("{");
  for (p = 0; p < s_taskPackageRecords.length; p++) {
    if (p != 0)
      str.append(", ");
    str.append(s_taskPackageRecords[p]);
  }
  str.append("}");
  outputted++;

  str.append("}");

  return str.toString();
}

//----------------------------------------------------------------
/*
 * Internal variables for class.
 */

public ASN1Integer s_updateStatus;
public DiagRec s_globalDiagnostics[]; // optional
public TaskPackageRecordStructure s_taskPackageRecords[];

//----------------------------------------------------------------
/*
 * Enumerated constants for class.
 */

// Enumerated constants for updateStatus
public static final int E_success = 1;
public static final int E_partial = 2;
public static final int E_failure = 3;

} // TargetPart

//----------------------------------------------------------------
//EOF
