/*
 * $Source$
 * $Date$
 * $Revision$
 *
 * Copyright (C) 1998, Hoylen Sue.  All Rights Reserved.
 * <h.sue@ieee.org>
 *
 * This program is distributed in the hope that it will be useful,
 * but WITHOUT ANY WARRANTY; without even the implied warranty of
 * MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  Refer to
 * the supplied license for more details.
 *
 * Generated by Zebulun ASN1tojava: 1998-09-08 03:20:30 UTC
 */

//----------------------------------------------------------------

package z3950.CIP2_4FormattedSceneSelectionOptions;
import asn1.*;
import z3950.v3.InternationalString;
import z3950.v3.IntUnit;

//================================================================
/**
 * Class for representing a <code>HorizontalSelection</code> from <code>CIP2-4-Order-ES</code>
 *
 * <pre>
 * HorizontalSelection ::=
 * CHOICE {
 *   rectangleSceneSelection [1] EXPLICIT RectangleSceneSelection
 *   boundingRectangleSelection [2] EXPLICIT BoundingRectangleSelection
 *   polygonSelection [3] EXPLICIT PolygonSelection
 *   circleSelection [4] EXPLICIT CircleSelection
 * }
 * </pre>
 *
 * @version	$Release$ $Date$
 */

//----------------------------------------------------------------

public final class HorizontalSelection extends ASN1Any
{

  public final static String VERSION = "Copyright (C) Hoylen Sue, 1998. 199809080320Z";

//----------------------------------------------------------------
/**
 * Default constructor for a HorizontalSelection.
 */

public
HorizontalSelection()
{
}

//----------------------------------------------------------------
/**
 * Constructor for a HorizontalSelection from a BER encoding.
 * <p>
 *
 * @param ber the BER encoding.
 * @param check_tag will check tag if true, use false
 *         if the BER has been implicitly tagged. You should
 *         usually be passing true.
 * @exception	ASN1Exception if the BER encoding is bad.
 */

public
HorizontalSelection(BEREncoding ber, boolean check_tag)
       throws ASN1Exception
{
  super(ber, check_tag);
}

//----------------------------------------------------------------
/**
 * Initializing object from a BER encoding.
 * This method is for internal use only. You should use
 * the constructor that takes a BEREncoding.
 *
 * @param ber the BER to decode.
 * @param check_tag if the tag should be checked.
 * @exception ASN1Exception if the BER encoding is bad.
 */

public void
ber_decode(BEREncoding ber, boolean check_tag)
       throws ASN1Exception
{
  BERConstructed tagwrapper;

  // Null out all choices
  c_rectangleSceneSelection = null;
  c_boundingRectangleSelection = null;
  c_polygonSelection = null;
  c_circleSelection = null;

  // Try choice rectangleSceneSelection
  if (ber.tag_get() == 1 &&
      ber.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    try {
      tagwrapper = (BERConstructed) ber;
    } catch (ClassCastException e) {
      throw new ASN1EncodingException
        ("Zebulun HorizontalSelection: bad BER form\n");
    }
    if (tagwrapper.number_components() != 1)
      throw new ASN1EncodingException
        ("Zebulun HorizontalSelection: bad BER form\n");
    c_rectangleSceneSelection = new RectangleSceneSelection(tagwrapper.elementAt(0), true);
    return;
  }

  // Try choice boundingRectangleSelection
  if (ber.tag_get() == 2 &&
      ber.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    try {
      tagwrapper = (BERConstructed) ber;
    } catch (ClassCastException e) {
      throw new ASN1EncodingException
        ("Zebulun HorizontalSelection: bad BER form\n");
    }
    if (tagwrapper.number_components() != 1)
      throw new ASN1EncodingException
        ("Zebulun HorizontalSelection: bad BER form\n");
    c_boundingRectangleSelection = new BoundingRectangleSelection(tagwrapper.elementAt(0), true);
    return;
  }

  // Try choice polygonSelection
  if (ber.tag_get() == 3 &&
      ber.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    try {
      tagwrapper = (BERConstructed) ber;
    } catch (ClassCastException e) {
      throw new ASN1EncodingException
        ("Zebulun HorizontalSelection: bad BER form\n");
    }
    if (tagwrapper.number_components() != 1)
      throw new ASN1EncodingException
        ("Zebulun HorizontalSelection: bad BER form\n");
    c_polygonSelection = new PolygonSelection(tagwrapper.elementAt(0), true);
    return;
  }

  // Try choice circleSelection
  if (ber.tag_get() == 4 &&
      ber.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    try {
      tagwrapper = (BERConstructed) ber;
    } catch (ClassCastException e) {
      throw new ASN1EncodingException
        ("Zebulun HorizontalSelection: bad BER form\n");
    }
    if (tagwrapper.number_components() != 1)
      throw new ASN1EncodingException
        ("Zebulun HorizontalSelection: bad BER form\n");
    c_circleSelection = new CircleSelection(tagwrapper.elementAt(0), true);
    return;
  }

  throw new ASN1Exception("Zebulun HorizontalSelection: bad BER encoding: choice not matched");
}

//----------------------------------------------------------------
/**
 * Returns a BER encoding of HorizontalSelection.
 *
 * @return	The BER encoding.
 * @exception	ASN1Exception Invalid or cannot be encoded.
 */

public BEREncoding
ber_encode()
       throws ASN1Exception
{
  BEREncoding chosen = null;

  BEREncoding enc[];

  // Encoding choice: c_rectangleSceneSelection
  if (c_rectangleSceneSelection != null) {
    enc = new BEREncoding[1];
    enc[0] = c_rectangleSceneSelection.ber_encode();
    chosen = new BERConstructed(BEREncoding.CONTEXT_SPECIFIC_TAG, 1, enc);
  }

  // Encoding choice: c_boundingRectangleSelection
  if (c_boundingRectangleSelection != null) {
    if (chosen != null)
      throw new ASN1Exception("CHOICE multiply set");
    enc = new BEREncoding[1];
    enc[0] = c_boundingRectangleSelection.ber_encode();
    chosen = new BERConstructed(BEREncoding.CONTEXT_SPECIFIC_TAG, 2, enc);
  }

  // Encoding choice: c_polygonSelection
  if (c_polygonSelection != null) {
    if (chosen != null)
      throw new ASN1Exception("CHOICE multiply set");
    enc = new BEREncoding[1];
    enc[0] = c_polygonSelection.ber_encode();
    chosen = new BERConstructed(BEREncoding.CONTEXT_SPECIFIC_TAG, 3, enc);
  }

  // Encoding choice: c_circleSelection
  if (c_circleSelection != null) {
    if (chosen != null)
      throw new ASN1Exception("CHOICE multiply set");
    enc = new BEREncoding[1];
    enc[0] = c_circleSelection.ber_encode();
    chosen = new BERConstructed(BEREncoding.CONTEXT_SPECIFIC_TAG, 4, enc);
  }

  // Check for error of having none of the choices set
  if (chosen == null)
    throw new ASN1Exception("CHOICE not set");

  return chosen;
}

//----------------------------------------------------------------

/**
 * Generating a BER encoding of the object
 * and implicitly tagging it.
 * <p>
 * This method is for internal use only. You should use
 * the ber_encode method that does not take a parameter.
 * <p>
 * This function should never be used, because this
 * production is a CHOICE.
 * It must never have an implicit tag.
 * <p>
 * An exception will be thrown if it is called.
 *
 * @param tag_type the type of the tag.
 * @param tag the tag.
 * @exception ASN1Exception if it cannot be BER encoded.
 */

public BEREncoding
ber_encode(int tag_type, int tag)
       throws ASN1Exception
{
  // This method must not be called!

  // Method is not available because this is a basic CHOICE
  // which does not have an explicit tag on it. So it is not
  // permitted to allow something else to apply an implicit
  // tag on it, otherwise the tag identifying which CHOICE
  // it is will be overwritten and lost.

  throw new ASN1EncodingException("Zebulun HorizontalSelection: cannot implicitly tag");
}

//----------------------------------------------------------------
/**
 * Returns a new String object containing a text representing
 * of the HorizontalSelection. 
 */

public String
toString()
{
  StringBuffer str = new StringBuffer("{");

  boolean found = false;

  if (c_rectangleSceneSelection != null) {
    if (found)
      str.append("<ERROR: multiple CHOICE: rectangleSceneSelection> ");
    found = true;
    str.append("rectangleSceneSelection ");
  str.append(c_rectangleSceneSelection);
  }

  if (c_boundingRectangleSelection != null) {
    if (found)
      str.append("<ERROR: multiple CHOICE: boundingRectangleSelection> ");
    found = true;
    str.append("boundingRectangleSelection ");
  str.append(c_boundingRectangleSelection);
  }

  if (c_polygonSelection != null) {
    if (found)
      str.append("<ERROR: multiple CHOICE: polygonSelection> ");
    found = true;
    str.append("polygonSelection ");
  str.append(c_polygonSelection);
  }

  if (c_circleSelection != null) {
    if (found)
      str.append("<ERROR: multiple CHOICE: circleSelection> ");
    found = true;
    str.append("circleSelection ");
  str.append(c_circleSelection);
  }

  str.append("}");

  return str.toString();
}

//----------------------------------------------------------------
/*
 * Internal variables for class.
 */
public RectangleSceneSelection c_rectangleSceneSelection;
public BoundingRectangleSelection c_boundingRectangleSelection;
public PolygonSelection c_polygonSelection;
public CircleSelection c_circleSelection;

} // HorizontalSelection

//----------------------------------------------------------------
//EOF
