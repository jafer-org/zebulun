/*
 * $Source$
 * $Date$
 * $Revision$
 *
 * Copyright (C) 1998, Hoylen Sue.  All Rights Reserved.
 * <h.sue@ieee.org>
 *
 * This program is distributed in the hope that it will be useful,
 * but WITHOUT ANY WARRANTY; without even the implied warranty of
 * MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  Refer to
 * the supplied license for more details.
 *
 * Generated by Zebulun ASN1tojava: 1998-09-08 03:20:29 UTC
 */

//----------------------------------------------------------------

package z3950.CIP2_4SpecificInfo;

import asn1.*;

import z3950.v3.IntUnit;
import z3950.v3.InternationalString;
import z3950.v3.Unit;
import z3950.v3.ResourceReport;

//================================================================
/**
 * Class for representing a <code>CIPSpecificInfo</code> from <code>CIP2-4-Release-B-APDU</code>
 *
 * <pre>
 * CIPSpecificInfo ::=
 * CHOICE {
 *   searchControl [1] IMPLICIT SearchControl
 *   term [2] EXPLICIT CIPTerm
 *   explainInfo [3] IMPLICIT AdditionalSemanticAttributes
 *   childrenResourceReport [4] EXPLICIT ChildrenResourceReport
 * }
 * </pre>
 *
 * @version	$Release$ $Date$
 */

//----------------------------------------------------------------

public final class CIPSpecificInfo extends ASN1Any
{

  public final static String VERSION = "Copyright (C) Hoylen Sue, 1998. 199809080320Z";

//----------------------------------------------------------------
/**
 * Default constructor for a CIPSpecificInfo.
 */

public
CIPSpecificInfo()
{
}

//----------------------------------------------------------------
/**
 * Constructor for a CIPSpecificInfo from a BER encoding.
 * <p>
 *
 * @param ber the BER encoding.
 * @param check_tag will check tag if true, use false
 *         if the BER has been implicitly tagged. You should
 *         usually be passing true.
 * @exception	ASN1Exception if the BER encoding is bad.
 */

public
CIPSpecificInfo(BEREncoding ber, boolean check_tag)
       throws ASN1Exception
{
  super(ber, check_tag);
}

//----------------------------------------------------------------
/**
 * Initializing object from a BER encoding.
 * This method is for internal use only. You should use
 * the constructor that takes a BEREncoding.
 *
 * @param ber the BER to decode.
 * @param check_tag if the tag should be checked.
 * @exception ASN1Exception if the BER encoding is bad.
 */

public void
ber_decode(BEREncoding ber, boolean check_tag)
       throws ASN1Exception
{
  BERConstructed tagwrapper;

  // Null out all choices

  c_searchControl = null;
  c_term = null;
  c_explainInfo = null;
  c_childrenResourceReport = null;

  // Try choice searchControl
  if (ber.tag_get() == 1 &&
      ber.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    c_searchControl = new SearchControl(ber, false);
    return;
  }

  // Try choice term
  if (ber.tag_get() == 2 &&
      ber.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    try {
      tagwrapper = (BERConstructed) ber;
    } catch (ClassCastException e) {
      throw new ASN1EncodingException
        ("Zebulun CIPSpecificInfo: bad BER form\n");
    }
    if (tagwrapper.number_components() != 1)
      throw new ASN1EncodingException
        ("Zebulun CIPSpecificInfo: bad BER form\n");
    c_term = new CIPTerm(tagwrapper.elementAt(0), true);
    return;
  }

  // Try choice explainInfo
  if (ber.tag_get() == 3 &&
      ber.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    c_explainInfo = new AdditionalSemanticAttributes(ber, false);
    return;
  }

  // Try choice childrenResourceReport
  if (ber.tag_get() == 4 &&
      ber.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    try {
      tagwrapper = (BERConstructed) ber;
    } catch (ClassCastException e) {
      throw new ASN1EncodingException
        ("Zebulun CIPSpecificInfo: bad BER form\n");
    }
    if (tagwrapper.number_components() != 1)
      throw new ASN1EncodingException
        ("Zebulun CIPSpecificInfo: bad BER form\n");
    c_childrenResourceReport = new ChildrenResourceReport(tagwrapper.elementAt(0), true);
    return;
  }

  throw new ASN1Exception("Zebulun CIPSpecificInfo: bad BER encoding: choice not matched");
}

//----------------------------------------------------------------
/**
 * Returns a BER encoding of CIPSpecificInfo.
 *
 * @return	The BER encoding.
 * @exception	ASN1Exception Invalid or cannot be encoded.
 */

public BEREncoding
ber_encode()
       throws ASN1Exception
{
  BEREncoding chosen = null;

  BEREncoding enc[];

  // Encoding choice: c_searchControl
  if (c_searchControl != null) {
    chosen = c_searchControl.ber_encode(BEREncoding.CONTEXT_SPECIFIC_TAG, 1);
  }

  // Encoding choice: c_term
  if (c_term != null) {
    if (chosen != null)
      throw new ASN1Exception("CHOICE multiply set");
    enc = new BEREncoding[1];
    enc[0] = c_term.ber_encode();
    chosen = new BERConstructed(BEREncoding.CONTEXT_SPECIFIC_TAG, 2, enc);
  }

  // Encoding choice: c_explainInfo
  if (c_explainInfo != null) {
    if (chosen != null)
      throw new ASN1Exception("CHOICE multiply set");
    chosen = c_explainInfo.ber_encode(BEREncoding.CONTEXT_SPECIFIC_TAG, 3);
  }

  // Encoding choice: c_childrenresourcereport
  if (c_childrenResourceReport != null) {
    if (chosen != null)
      throw new ASN1Exception("CHOICE multiply set");
    enc = new BEREncoding[1];
    enc[0] = c_childrenResourceReport.ber_encode();
    chosen = new BERConstructed(BEREncoding.CONTEXT_SPECIFIC_TAG, 4, enc);
  }

  // Check for error of having none of the choices set
  if (chosen == null)
    throw new ASN1Exception("CHOICE not set");

  return chosen;
}

//----------------------------------------------------------------

/**
 * Generating a BER encoding of the object
 * and implicitly tagging it.
 * <p>
 * This method is for internal use only. You should use
 * the ber_encode method that does not take a parameter.
 * <p>
 * This function should never be used, because this
 * production is a CHOICE.
 * It must never have an implicit tag.
 * <p>
 * An exception will be thrown if it is called.
 *
 * @param tag_type the type of the tag.
 * @param tag the tag.
 * @exception ASN1Exception if it cannot be BER encoded.
 */

public BEREncoding
ber_encode(int tag_type, int tag)
       throws ASN1Exception
{
  // This method must not be called!

  // Method is not available because this is a basic CHOICE
  // which does not have an explicit tag on it. So it is not
  // permitted to allow something else to apply an implicit
  // tag on it, otherwise the tag identifying which CHOICE
  // it is will be overwritten and lost.

  throw new ASN1EncodingException("Zebulun CIPSpecificInfo: cannot implicitly tag");
}

//----------------------------------------------------------------
/**
 * Returns a new String object containing a text representing
 * of the CIPSpecificInfo. 
 */

public String
toString()
{
  StringBuffer str = new StringBuffer("{");

  boolean found = false;

  if (c_searchControl != null) {
    if (found)
      str.append("<ERROR: multiple CHOICE: searchControl> ");
    found = true;
    str.append("searchControl ");
  str.append(c_searchControl);
  }

  if (c_term != null) {
    if (found)
      str.append("<ERROR: multiple CHOICE: term> ");
    found = true;
    str.append("term ");
  str.append(c_term);
  }

  if (c_explainInfo != null) {
    if (found)
      str.append("<ERROR: multiple CHOICE: explainInfo> ");
    found = true;
    str.append("explainInfo ");
  str.append(c_explainInfo);
  }

  if (c_childrenResourceReport != null) {
    if (found)
      str.append("<ERROR: multiple CHOICE: term> ");
    found = true;
    str.append("childrenResourceReport ");
  str.append(c_childrenResourceReport);
  }

  str.append("}");

  return str.toString();
}

//----------------------------------------------------------------
/*
 * Internal variables for class.
 */

public SearchControl c_searchControl;
public CIPTerm c_term;
public AdditionalSemanticAttributes c_explainInfo;
public ChildrenResourceReport c_childrenResourceReport;

} // CIPSpecificInfo

//----------------------------------------------------------------
//EOF
