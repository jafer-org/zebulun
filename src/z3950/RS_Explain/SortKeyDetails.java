/*
 * $Source$
 * $Date$
 * $Revision$
 *
 * Copyright (C) 1998, Hoylen Sue.  All Rights Reserved.
 * <h.sue@ieee.org>
 *
 * This program is distributed in the hope that it will be useful,
 * but WITHOUT ANY WARRANTY; without even the implied warranty of
 * MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  Refer to
 * the supplied license for more details.
 *
 * Generated by Zebulun ASN1tojava: 1998-09-08 03:15:21 UTC
 */

//----------------------------------------------------------------

package z3950.RS_Explain;
import asn1.*;
import z3950.v3.AttributeElement;
import z3950.v3.AttributeList;
import z3950.v3.AttributeSetId;
import z3950.v3.DatabaseName;
import z3950.v3.ElementSetName;
import z3950.v3.IntUnit;
import z3950.v3.InternationalString;
import z3950.v3.OtherInformation;
import z3950.v3.Specification;
import z3950.v3.StringOrNumeric;
import z3950.v3.Term;
import z3950.v3.Unit;

//================================================================
/**
 * Class for representing a <code>SortKeyDetails</code> from <code>RecordSyntax-explain</code>
 *
 * <pre>
 * SortKeyDetails ::=
 * SEQUENCE {
 *   description [0] IMPLICIT HumanString OPTIONAL
 *   elementSpecifications [1] IMPLICIT SEQUENCE OF Specification OPTIONAL
 *   attributeSpecifications [2] IMPLICIT AttributeCombinations OPTIONAL
 *   sortType [3] EXPLICIT SortKeyDetails_sortType OPTIONAL
 *   caseSensitivity [4] IMPLICIT INTEGER OPTIONAL
 * }
 * </pre>
 *
 * @version	$Release$ $Date$
 */

//----------------------------------------------------------------

public final class SortKeyDetails extends ASN1Any
{

  public final static String VERSION = "Copyright (C) Hoylen Sue, 1998. 199809080315Z";

//----------------------------------------------------------------
/**
 * Default constructor for a SortKeyDetails.
 */

public
SortKeyDetails()
{
}

//----------------------------------------------------------------
/**
 * Constructor for a SortKeyDetails from a BER encoding.
 * <p>
 *
 * @param ber the BER encoding.
 * @param check_tag will check tag if true, use false
 *         if the BER has been implicitly tagged. You should
 *         usually be passing true.
 * @exception	ASN1Exception if the BER encoding is bad.
 */

public
SortKeyDetails(BEREncoding ber, boolean check_tag)
       throws ASN1Exception
{
  super(ber, check_tag);
}

//----------------------------------------------------------------
/**
 * Initializing object from a BER encoding.
 * This method is for internal use only. You should use
 * the constructor that takes a BEREncoding.
 *
 * @param ber the BER to decode.
 * @param check_tag if the tag should be checked.
 * @exception ASN1Exception if the BER encoding is bad.
 */

public void
ber_decode(BEREncoding ber, boolean check_tag)
       throws ASN1Exception
{
  // SortKeyDetails should be encoded by a constructed BER

  BERConstructed ber_cons;
  try {
    ber_cons = (BERConstructed) ber;
  } catch (ClassCastException e) {
    throw new ASN1EncodingException
      ("Zebulun SortKeyDetails: bad BER form\n");
  }

  // Prepare to decode the components

  int num_parts = ber_cons.number_components();
  int part = 0;
  BEREncoding p;
  BERConstructed tagged;

  // Remaining elements are optional, set variables
  // to null (not present) so can return at end of BER

  s_description = null;
  s_elementSpecifications = null;
  s_attributeSpecifications = null;
  s_sortType = null;
  s_caseSensitivity = null;

  // Decoding: description [0] IMPLICIT HumanString OPTIONAL

  if (num_parts <= part) {
    return; // no more data, but ok (rest is optional)
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() == 0 &&
      p.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    s_description = new HumanString(p, false);
    part++;
  }

  // Decoding: elementSpecifications [1] IMPLICIT SEQUENCE OF Specification OPTIONAL

  if (num_parts <= part) {
    return; // no more data, but ok (rest is optional)
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() == 1 &&
      p.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    try {
      BERConstructed cons = (BERConstructed) p;
      int parts = cons.number_components();
      s_elementSpecifications = new Specification[parts];
      int n;
      for (n = 0; n < parts; n++) {
        s_elementSpecifications[n] = new Specification(cons.elementAt(n), true);
      }
    } catch (ClassCastException e) {
      throw new ASN1EncodingException("Bad BER");
    }
    part++;
  }

  // Decoding: attributeSpecifications [2] IMPLICIT AttributeCombinations OPTIONAL

  if (num_parts <= part) {
    return; // no more data, but ok (rest is optional)
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() == 2 &&
      p.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    s_attributeSpecifications = new AttributeCombinations(p, false);
    part++;
  }

  // Decoding: sortType [3] EXPLICIT SortKeyDetails_sortType OPTIONAL

  if (num_parts <= part) {
    return; // no more data, but ok (rest is optional)
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() == 3 &&
      p.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    try {
      tagged = (BERConstructed) p;
    } catch (ClassCastException e) {
      throw new ASN1EncodingException
        ("Zebulun SortKeyDetails: bad BER encoding: s_sortType tag bad\n");
    }
    if (tagged.number_components() != 1) {
      throw new ASN1EncodingException
        ("Zebulun SortKeyDetails: bad BER encoding: s_sortType tag bad\n");
    }

    s_sortType = new SortKeyDetails_sortType(tagged.elementAt(0), true);
    part++;
  }

  // Decoding: caseSensitivity [4] IMPLICIT INTEGER OPTIONAL

  if (num_parts <= part) {
    return; // no more data, but ok (rest is optional)
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() == 4 &&
      p.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    s_caseSensitivity = new ASN1Integer(p, false);
    part++;
  }

  // Should not be any more parts

  if (part < num_parts) {
    throw new ASN1Exception("Zebulun SortKeyDetails: bad BER: extra data " + part + "/" + num_parts + " processed");
  }
}

//----------------------------------------------------------------
/**
 * Returns a BER encoding of the SortKeyDetails.
 *
 * @exception	ASN1Exception Invalid or cannot be encoded.
 * @return	The BER encoding.
 */

public BEREncoding
ber_encode()
       throws ASN1Exception
{
  return ber_encode(BEREncoding.UNIVERSAL_TAG, ASN1Sequence.TAG);
}

//----------------------------------------------------------------
/**
 * Returns a BER encoding of SortKeyDetails, implicitly tagged.
 *
 * @param tag_type	The type of the implicit tag.
 * @param tag	The implicit tag.
 * @return	The BER encoding of the object.
 * @exception	ASN1Exception When invalid or cannot be encoded.
 * @see asn1.BEREncoding#UNIVERSAL_TAG
 * @see asn1.BEREncoding#APPLICATION_TAG
 * @see asn1.BEREncoding#CONTEXT_SPECIFIC_TAG
 * @see asn1.BEREncoding#PRIVATE_TAG
 */

public BEREncoding
ber_encode(int tag_type, int tag)
       throws ASN1Exception
{
  // Calculate the number of fields in the encoding

  int num_fields = 0; // number of mandatories
  if (s_description != null)
    num_fields++;
  if (s_elementSpecifications != null)
    num_fields++;
  if (s_attributeSpecifications != null)
    num_fields++;
  if (s_sortType != null)
    num_fields++;
  if (s_caseSensitivity != null)
    num_fields++;

  // Encode it

  BEREncoding fields[] = new BEREncoding[num_fields];
  int x = 0;
  BEREncoding f2[];
  int p;
  BEREncoding enc[];

  // Encoding s_description: HumanString OPTIONAL

  if (s_description != null) {
    fields[x++] = s_description.ber_encode(BEREncoding.CONTEXT_SPECIFIC_TAG, 0);
  }

  // Encoding s_elementSpecifications: SEQUENCE OF OPTIONAL

  if (s_elementSpecifications != null) {
    f2 = new BEREncoding[s_elementSpecifications.length];

    for (p = 0; p < s_elementSpecifications.length; p++) {
      f2[p] = s_elementSpecifications[p].ber_encode();
    }

    fields[x++] = new BERConstructed(BEREncoding.CONTEXT_SPECIFIC_TAG, 1, f2);
  }

  // Encoding s_attributeSpecifications: AttributeCombinations OPTIONAL

  if (s_attributeSpecifications != null) {
    fields[x++] = s_attributeSpecifications.ber_encode(BEREncoding.CONTEXT_SPECIFIC_TAG, 2);
  }

  // Encoding s_sortType: SortKeyDetails_sortType OPTIONAL

  if (s_sortType != null) {
    enc = new BEREncoding[1];
    enc[0] = s_sortType.ber_encode();
    fields[x++] = new BERConstructed(BEREncoding.CONTEXT_SPECIFIC_TAG, 3, enc);
  }

  // Encoding s_caseSensitivity: INTEGER OPTIONAL

  if (s_caseSensitivity != null) {
    fields[x++] = s_caseSensitivity.ber_encode(BEREncoding.CONTEXT_SPECIFIC_TAG, 4);
  }

  return new BERConstructed(tag_type, tag, fields);
}

//----------------------------------------------------------------
/**
 * Returns a new String object containing a text representing
 * of the SortKeyDetails. 
 */

public String
toString()
{
  int p;
  StringBuffer str = new StringBuffer("{");
  int outputted = 0;

  if (s_description != null) {
    str.append("description ");
    str.append(s_description);
    outputted++;
  }

  if (s_elementSpecifications != null) {
    if (0 < outputted)
    str.append(", ");
    str.append("elementSpecifications ");
    str.append("{");
    for (p = 0; p < s_elementSpecifications.length; p++) {
      if (p != 0)
        str.append(", ");
      str.append(s_elementSpecifications[p]);
    }
    str.append("}");
    outputted++;
  }

  if (s_attributeSpecifications != null) {
    if (0 < outputted)
    str.append(", ");
    str.append("attributeSpecifications ");
    str.append(s_attributeSpecifications);
    outputted++;
  }

  if (s_sortType != null) {
    if (0 < outputted)
    str.append(", ");
    str.append("sortType ");
    str.append(s_sortType);
    outputted++;
  }

  if (s_caseSensitivity != null) {
    if (0 < outputted)
    str.append(", ");
    str.append("caseSensitivity ");
    str.append(s_caseSensitivity);
    outputted++;
  }

  str.append("}");

  return str.toString();
}

//----------------------------------------------------------------
/*
 * Internal variables for class.
 */

public HumanString s_description; // optional
public Specification s_elementSpecifications[]; // optional
public AttributeCombinations s_attributeSpecifications; // optional
public SortKeyDetails_sortType s_sortType; // optional
public ASN1Integer s_caseSensitivity; // optional

//----------------------------------------------------------------
/*
 * Enumerated constants for class.
 */

// Enumerated constants for caseSensitivity
public static final int E_always = 0;
public static final int E_never = 1;
public static final int E_default_yes = 2;
public static final int E_default_no = 3;

} // SortKeyDetails

//----------------------------------------------------------------
//EOF
