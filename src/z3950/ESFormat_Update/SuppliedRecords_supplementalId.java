/*
 * $Source$
 * $Date$
 * $Revision$
 *
 * Copyright (C) 1998, Hoylen Sue.  All Rights Reserved.
 * <h.sue@ieee.org>
 *
 * This program is distributed in the hope that it will be useful,
 * but WITHOUT ANY WARRANTY; without even the implied warranty of
 * MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  Refer to
 * the supplied license for more details.
 *
 * Generated by Zebulun ASN1tojava: 1998-09-08 03:15:27 UTC
 */

//----------------------------------------------------------------

package z3950.ESFormat_Update;
import asn1.*;
import z3950.v3.DiagRec;
import z3950.v3.InternationalString;

//================================================================
/**
 * Class for representing a <code>SuppliedRecords_supplementalId</code> from <code>ESFormat-Update</code>
 *
 * <pre>
 * SuppliedRecords_supplementalId ::=
 * CHOICE {
 *   timeStamp [1] IMPLICIT GeneralizedTime
 *   versionNumber [2] IMPLICIT InternationalString
 *   previousVersion [3] IMPLICIT EXTERNAL
 * }
 * </pre>
 *
 * @version	$Release$ $Date$
 */

//----------------------------------------------------------------

public final class SuppliedRecords_supplementalId extends ASN1Any
{

  public final static String VERSION = "Copyright (C) Hoylen Sue, 1998. 199809080315Z";

//----------------------------------------------------------------
/**
 * Default constructor for a SuppliedRecords_supplementalId.
 */

public
SuppliedRecords_supplementalId()
{
}

//----------------------------------------------------------------
/**
 * Constructor for a SuppliedRecords_supplementalId from a BER encoding.
 * <p>
 *
 * @param ber the BER encoding.
 * @param check_tag will check tag if true, use false
 *         if the BER has been implicitly tagged. You should
 *         usually be passing true.
 * @exception	ASN1Exception if the BER encoding is bad.
 */

public
SuppliedRecords_supplementalId(BEREncoding ber, boolean check_tag)
       throws ASN1Exception
{
  super(ber, check_tag);
}

//----------------------------------------------------------------
/**
 * Initializing object from a BER encoding.
 * This method is for internal use only. You should use
 * the constructor that takes a BEREncoding.
 *
 * @param ber the BER to decode.
 * @param check_tag if the tag should be checked.
 * @exception ASN1Exception if the BER encoding is bad.
 */

public void
ber_decode(BEREncoding ber, boolean check_tag)
       throws ASN1Exception
{
  // Null out all choices

  c_timeStamp = null;
  c_versionNumber = null;
  c_previousVersion = null;

  // Try choice timeStamp
  if (ber.tag_get() == 1 &&
      ber.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    c_timeStamp = new ASN1GeneralizedTime(ber, false);
    return;
  }

  // Try choice versionNumber
  if (ber.tag_get() == 2 &&
      ber.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    c_versionNumber = new InternationalString(ber, false);
    return;
  }

  // Try choice previousVersion
  if (ber.tag_get() == 3 &&
      ber.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    c_previousVersion = new ASN1External(ber, false);
    return;
  }

  throw new ASN1Exception("Zebulun SuppliedRecords_supplementalId: bad BER encoding: choice not matched");
}

//----------------------------------------------------------------
/**
 * Returns a BER encoding of SuppliedRecords_supplementalId.
 *
 * @return	The BER encoding.
 * @exception	ASN1Exception Invalid or cannot be encoded.
 */

public BEREncoding
ber_encode()
       throws ASN1Exception
{
  BEREncoding chosen = null;

  // Encoding choice: c_timeStamp
  if (c_timeStamp != null) {
    chosen = c_timeStamp.ber_encode(BEREncoding.CONTEXT_SPECIFIC_TAG, 1);
  }

  // Encoding choice: c_versionNumber
  if (c_versionNumber != null) {
    if (chosen != null)
      throw new ASN1Exception("CHOICE multiply set");
    chosen = c_versionNumber.ber_encode(BEREncoding.CONTEXT_SPECIFIC_TAG, 2);
  }

  // Encoding choice: c_previousVersion
  if (c_previousVersion != null) {
    if (chosen != null)
      throw new ASN1Exception("CHOICE multiply set");
    chosen = c_previousVersion.ber_encode(BEREncoding.CONTEXT_SPECIFIC_TAG, 3);
  }

  // Check for error of having none of the choices set
  if (chosen == null)
    throw new ASN1Exception("CHOICE not set");

  return chosen;
}

//----------------------------------------------------------------

/**
 * Generating a BER encoding of the object
 * and implicitly tagging it.
 * <p>
 * This method is for internal use only. You should use
 * the ber_encode method that does not take a parameter.
 * <p>
 * This function should never be used, because this
 * production is a CHOICE.
 * It must never have an implicit tag.
 * <p>
 * An exception will be thrown if it is called.
 *
 * @param tag_type the type of the tag.
 * @param tag the tag.
 * @exception ASN1Exception if it cannot be BER encoded.
 */

public BEREncoding
ber_encode(int tag_type, int tag)
       throws ASN1Exception
{
  // This method must not be called!

  // Method is not available because this is a basic CHOICE
  // which does not have an explicit tag on it. So it is not
  // permitted to allow something else to apply an implicit
  // tag on it, otherwise the tag identifying which CHOICE
  // it is will be overwritten and lost.

  throw new ASN1EncodingException("Zebulun SuppliedRecords_supplementalId: cannot implicitly tag");
}

//----------------------------------------------------------------
/**
 * Returns a new String object containing a text representing
 * of the SuppliedRecords_supplementalId. 
 */

public String
toString()
{
  StringBuffer str = new StringBuffer("{");

  boolean found = false;

  if (c_timeStamp != null) {
    if (found)
      str.append("<ERROR: multiple CHOICE: timeStamp> ");
    found = true;
    str.append("timeStamp ");
  str.append(c_timeStamp);
  }

  if (c_versionNumber != null) {
    if (found)
      str.append("<ERROR: multiple CHOICE: versionNumber> ");
    found = true;
    str.append("versionNumber ");
  str.append(c_versionNumber);
  }

  if (c_previousVersion != null) {
    if (found)
      str.append("<ERROR: multiple CHOICE: previousVersion> ");
    found = true;
    str.append("previousVersion ");
  str.append(c_previousVersion);
  }

  str.append("}");

  return str.toString();
}

//----------------------------------------------------------------
/*
 * Internal variables for class.
 */

public ASN1GeneralizedTime c_timeStamp;
public InternationalString c_versionNumber;
public ASN1External c_previousVersion;

} // SuppliedRecords_supplementalId

//----------------------------------------------------------------
//EOF
