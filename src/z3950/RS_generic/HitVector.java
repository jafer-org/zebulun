/*
 * $Source$
 * $Date$
 * $Revision$
 *
 * Copyright (C) 1998, Hoylen Sue.  All Rights Reserved.
 * <h.sue@ieee.org>
 *
 * This program is distributed in the hope that it will be useful,
 * but WITHOUT ANY WARRANTY; without even the implied warranty of
 * MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  Refer to
 * the supplied license for more details.
 *
 * Generated by Zebulun ASN1tojava: 1998-09-08 03:15:23 UTC
 */

//----------------------------------------------------------------

package z3950.RS_generic;
import asn1.*;
import z3950.v3.IntUnit;
import z3950.v3.InternationalString;
import z3950.v3.StringOrNumeric;
import z3950.v3.Term;
import z3950.v3.Unit;

//================================================================
/**
 * Class for representing a <code>HitVector</code> from <code>RecordSyntax-generic</code>
 *
 * <pre>
 * HitVector ::=
 * SEQUENCE {
 *   satisfier Term OPTIONAL
 *   offsetIntoElement [1] IMPLICIT IntUnit OPTIONAL
 *   length [2] IMPLICIT IntUnit OPTIONAL
 *   hitRank [3] IMPLICIT INTEGER OPTIONAL
 *   targetToken [4] IMPLICIT OCTET STRING OPTIONAL
 * }
 * </pre>
 *
 * @version	$Release$ $Date$
 */

//----------------------------------------------------------------

public final class HitVector extends ASN1Any
{

  public final static String VERSION = "Copyright (C) Hoylen Sue, 1998. 199809080315Z";

//----------------------------------------------------------------
/**
 * Default constructor for a HitVector.
 */

public
HitVector()
{
}

//----------------------------------------------------------------
/**
 * Constructor for a HitVector from a BER encoding.
 * <p>
 *
 * @param ber the BER encoding.
 * @param check_tag will check tag if true, use false
 *         if the BER has been implicitly tagged. You should
 *         usually be passing true.
 * @exception	ASN1Exception if the BER encoding is bad.
 */

public
HitVector(BEREncoding ber, boolean check_tag)
       throws ASN1Exception
{
  super(ber, check_tag);
}

//----------------------------------------------------------------
/**
 * Initializing object from a BER encoding.
 * This method is for internal use only. You should use
 * the constructor that takes a BEREncoding.
 *
 * @param ber the BER to decode.
 * @param check_tag if the tag should be checked.
 * @exception ASN1Exception if the BER encoding is bad.
 */

public void
ber_decode(BEREncoding ber, boolean check_tag)
       throws ASN1Exception
{
  // HitVector should be encoded by a constructed BER

  BERConstructed ber_cons;
  try {
    ber_cons = (BERConstructed) ber;
  } catch (ClassCastException e) {
    throw new ASN1EncodingException
      ("Zebulun HitVector: bad BER form\n");
  }

  // Prepare to decode the components

  int num_parts = ber_cons.number_components();
  int part = 0;
  BEREncoding p;

  // Remaining elements are optional, set variables
  // to null (not present) so can return at end of BER

  s_satisfier = null;
  s_offsetIntoElement = null;
  s_length = null;
  s_hitRank = null;
  s_targetToken = null;

  // Decoding: satisfier Term OPTIONAL

  if (num_parts <= part) {
    return; // no more data, but ok (rest is optional)
  }
  p = ber_cons.elementAt(part);

  try {
    s_satisfier = new Term(p, true);
    part++; // yes, consumed
  } catch (ASN1Exception e) {
    s_satisfier = null; // no, not present
  }

  // Decoding: offsetIntoElement [1] IMPLICIT IntUnit OPTIONAL

  if (num_parts <= part) {
    return; // no more data, but ok (rest is optional)
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() == 1 &&
      p.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    s_offsetIntoElement = new IntUnit(p, false);
    part++;
  }

  // Decoding: length [2] IMPLICIT IntUnit OPTIONAL

  if (num_parts <= part) {
    return; // no more data, but ok (rest is optional)
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() == 2 &&
      p.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    s_length = new IntUnit(p, false);
    part++;
  }

  // Decoding: hitRank [3] IMPLICIT INTEGER OPTIONAL

  if (num_parts <= part) {
    return; // no more data, but ok (rest is optional)
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() == 3 &&
      p.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    s_hitRank = new ASN1Integer(p, false);
    part++;
  }

  // Decoding: targetToken [4] IMPLICIT OCTET STRING OPTIONAL

  if (num_parts <= part) {
    return; // no more data, but ok (rest is optional)
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() == 4 &&
      p.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    s_targetToken = new ASN1OctetString(p, false);
    part++;
  }

  // Should not be any more parts

  if (part < num_parts) {
    throw new ASN1Exception("Zebulun HitVector: bad BER: extra data " + part + "/" + num_parts + " processed");
  }
}

//----------------------------------------------------------------
/**
 * Returns a BER encoding of the HitVector.
 *
 * @exception	ASN1Exception Invalid or cannot be encoded.
 * @return	The BER encoding.
 */

public BEREncoding
ber_encode()
       throws ASN1Exception
{
  return ber_encode(BEREncoding.UNIVERSAL_TAG, ASN1Sequence.TAG);
}

//----------------------------------------------------------------
/**
 * Returns a BER encoding of HitVector, implicitly tagged.
 *
 * @param tag_type	The type of the implicit tag.
 * @param tag	The implicit tag.
 * @return	The BER encoding of the object.
 * @exception	ASN1Exception When invalid or cannot be encoded.
 * @see asn1.BEREncoding#UNIVERSAL_TAG
 * @see asn1.BEREncoding#APPLICATION_TAG
 * @see asn1.BEREncoding#CONTEXT_SPECIFIC_TAG
 * @see asn1.BEREncoding#PRIVATE_TAG
 */

public BEREncoding
ber_encode(int tag_type, int tag)
       throws ASN1Exception
{
  // Calculate the number of fields in the encoding

  int num_fields = 0; // number of mandatories
  if (s_satisfier != null)
    num_fields++;
  if (s_offsetIntoElement != null)
    num_fields++;
  if (s_length != null)
    num_fields++;
  if (s_hitRank != null)
    num_fields++;
  if (s_targetToken != null)
    num_fields++;

  // Encode it

  BEREncoding fields[] = new BEREncoding[num_fields];
  int x = 0;

  // Encoding s_satisfier: Term OPTIONAL

  if (s_satisfier != null) {
    fields[x++] = s_satisfier.ber_encode();
  }

  // Encoding s_offsetIntoElement: IntUnit OPTIONAL

  if (s_offsetIntoElement != null) {
    fields[x++] = s_offsetIntoElement.ber_encode(BEREncoding.CONTEXT_SPECIFIC_TAG, 1);
  }

  // Encoding s_length: IntUnit OPTIONAL

  if (s_length != null) {
    fields[x++] = s_length.ber_encode(BEREncoding.CONTEXT_SPECIFIC_TAG, 2);
  }

  // Encoding s_hitRank: INTEGER OPTIONAL

  if (s_hitRank != null) {
    fields[x++] = s_hitRank.ber_encode(BEREncoding.CONTEXT_SPECIFIC_TAG, 3);
  }

  // Encoding s_targetToken: OCTET STRING OPTIONAL

  if (s_targetToken != null) {
    fields[x++] = s_targetToken.ber_encode(BEREncoding.CONTEXT_SPECIFIC_TAG, 4);
  }

  return new BERConstructed(tag_type, tag, fields);
}

//----------------------------------------------------------------
/**
 * Returns a new String object containing a text representing
 * of the HitVector. 
 */

public String
toString()
{
  StringBuffer str = new StringBuffer("{");
  int outputted = 0;

  if (s_satisfier != null) {
    str.append("satisfier ");
    str.append(s_satisfier);
    outputted++;
  }

  if (s_offsetIntoElement != null) {
    if (0 < outputted)
    str.append(", ");
    str.append("offsetIntoElement ");
    str.append(s_offsetIntoElement);
    outputted++;
  }

  if (s_length != null) {
    if (0 < outputted)
    str.append(", ");
    str.append("length ");
    str.append(s_length);
    outputted++;
  }

  if (s_hitRank != null) {
    if (0 < outputted)
    str.append(", ");
    str.append("hitRank ");
    str.append(s_hitRank);
    outputted++;
  }

  if (s_targetToken != null) {
    if (0 < outputted)
    str.append(", ");
    str.append("targetToken ");
    str.append(s_targetToken);
    outputted++;
  }

  str.append("}");

  return str.toString();
}

//----------------------------------------------------------------
/*
 * Internal variables for class.
 */

public Term s_satisfier; // optional
public IntUnit s_offsetIntoElement; // optional
public IntUnit s_length; // optional
public ASN1Integer s_hitRank; // optional
public ASN1OctetString s_targetToken; // optional

} // HitVector

//----------------------------------------------------------------
//EOF
