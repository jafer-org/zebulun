/*
 * $Source$
 * $Date$
 * $Revision$
 *
 * Copyright (C) 1998, Hoylen Sue.  All Rights Reserved.
 * <h.sue@ieee.org>
 *
 * This program is distributed in the hope that it will be useful,
 * but WITHOUT ANY WARRANTY; without even the implied warranty of
 * MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  Refer to
 * the supplied license for more details.
 *
 * Generated by Zebulun ASN1tojava: 1998-09-08 03:15:23 UTC
 */

//----------------------------------------------------------------

package z3950.RS_generic;
import asn1.*;
import z3950.v3.IntUnit;
import z3950.v3.InternationalString;
import z3950.v3.StringOrNumeric;
import z3950.v3.Term;
import z3950.v3.Unit;

//================================================================
/**
 * Class for representing a <code>TaggedElement</code> from <code>RecordSyntax-generic</code>
 *
 * <pre>
 * TaggedElement ::=
 * SEQUENCE {
 *   tagType [1] IMPLICIT INTEGER OPTIONAL
 *   tagValue [2] EXPLICIT StringOrNumeric
 *   tagOccurrence [3] IMPLICIT INTEGER OPTIONAL
 *   content [4] EXPLICIT ElementData
 *   metaData [5] IMPLICIT ElementMetaData OPTIONAL
 *   appliedVariant [6] IMPLICIT Variant OPTIONAL
 * }
 * </pre>
 *
 * @version	$Release$ $Date$
 */

//----------------------------------------------------------------

public final class TaggedElement extends ASN1Any
{

  public final static String VERSION = "Copyright (C) Hoylen Sue, 1998. 199809080315Z";

//----------------------------------------------------------------
/**
 * Default constructor for a TaggedElement.
 */

public
TaggedElement()
{
}

//----------------------------------------------------------------
/**
 * Constructor for a TaggedElement from a BER encoding.
 * <p>
 *
 * @param ber the BER encoding.
 * @param check_tag will check tag if true, use false
 *         if the BER has been implicitly tagged. You should
 *         usually be passing true.
 * @exception	ASN1Exception if the BER encoding is bad.
 */

public
TaggedElement(BEREncoding ber, boolean check_tag)
       throws ASN1Exception
{
  super(ber, check_tag);
}

//----------------------------------------------------------------
/**
 * Initializing object from a BER encoding.
 * This method is for internal use only. You should use
 * the constructor that takes a BEREncoding.
 *
 * @param ber the BER to decode.
 * @param check_tag if the tag should be checked.
 * @exception ASN1Exception if the BER encoding is bad.
 */

public void
ber_decode(BEREncoding ber, boolean check_tag)
       throws ASN1Exception
{
  // TaggedElement should be encoded by a constructed BER

  BERConstructed ber_cons;
  try {
    ber_cons = (BERConstructed) ber;
  } catch (ClassCastException e) {
    throw new ASN1EncodingException
      ("Zebulun TaggedElement: bad BER form\n");
  }

  // Prepare to decode the components

  int num_parts = ber_cons.number_components();
  int part = 0;
  BEREncoding p;
  BERConstructed tagged;

  // Decoding: tagType [1] IMPLICIT INTEGER OPTIONAL

  if (num_parts <= part) {
    // End of record, but still more elements to get
    throw new ASN1Exception("Zebulun TaggedElement: incomplete");
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() == 1 &&
      p.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    s_tagType = new ASN1Integer(p, false);
    part++;
  }

  // Decoding: tagValue [2] EXPLICIT StringOrNumeric

  if (num_parts <= part) {
    // End of record, but still more elements to get
    throw new ASN1Exception("Zebulun TaggedElement: incomplete");
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() != 2 ||
      p.tag_type_get() != BEREncoding.CONTEXT_SPECIFIC_TAG)
    throw new ASN1EncodingException
      ("Zebulun TaggedElement: bad tag in s_tagValue\n");

  try {
    tagged = (BERConstructed) p;
  } catch (ClassCastException e) {
    throw new ASN1EncodingException
      ("Zebulun TaggedElement: bad BER encoding: s_tagValue tag bad\n");
  }
  if (tagged.number_components() != 1) {
    throw new ASN1EncodingException
      ("Zebulun TaggedElement: bad BER encoding: s_tagValue tag bad\n");
  }

  s_tagValue = new StringOrNumeric(tagged.elementAt(0), true);
  part++;

  // Decoding: tagOccurrence [3] IMPLICIT INTEGER OPTIONAL

  if (num_parts <= part) {
    // End of record, but still more elements to get
    throw new ASN1Exception("Zebulun TaggedElement: incomplete");
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() == 3 &&
      p.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    s_tagOccurrence = new ASN1Integer(p, false);
    part++;
  }

  // Decoding: content [4] EXPLICIT ElementData

  if (num_parts <= part) {
    // End of record, but still more elements to get
    throw new ASN1Exception("Zebulun TaggedElement: incomplete");
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() != 4 ||
      p.tag_type_get() != BEREncoding.CONTEXT_SPECIFIC_TAG)
    throw new ASN1EncodingException
      ("Zebulun TaggedElement: bad tag in s_content\n");

  try {
    tagged = (BERConstructed) p;
  } catch (ClassCastException e) {
    throw new ASN1EncodingException
      ("Zebulun TaggedElement: bad BER encoding: s_content tag bad\n");
  }
  if (tagged.number_components() != 1) {
    throw new ASN1EncodingException
      ("Zebulun TaggedElement: bad BER encoding: s_content tag bad\n");
  }

  s_content = new ElementData(tagged.elementAt(0), true);
  part++;

  // Remaining elements are optional, set variables
  // to null (not present) so can return at end of BER

  s_metaData = null;
  s_appliedVariant = null;

  // Decoding: metaData [5] IMPLICIT ElementMetaData OPTIONAL

  if (num_parts <= part) {
    return; // no more data, but ok (rest is optional)
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() == 5 &&
      p.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    s_metaData = new ElementMetaData(p, false);
    part++;
  }

  // Decoding: appliedVariant [6] IMPLICIT Variant OPTIONAL

  if (num_parts <= part) {
    return; // no more data, but ok (rest is optional)
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() == 6 &&
      p.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    s_appliedVariant = new Variant(p, false);
    part++;
  }

  // Should not be any more parts

  if (part < num_parts) {
    throw new ASN1Exception("Zebulun TaggedElement: bad BER: extra data " + part + "/" + num_parts + " processed");
  }
}

//----------------------------------------------------------------
/**
 * Returns a BER encoding of the TaggedElement.
 *
 * @exception	ASN1Exception Invalid or cannot be encoded.
 * @return	The BER encoding.
 */

public BEREncoding
ber_encode()
       throws ASN1Exception
{
  return ber_encode(BEREncoding.UNIVERSAL_TAG, ASN1Sequence.TAG);
}

//----------------------------------------------------------------
/**
 * Returns a BER encoding of TaggedElement, implicitly tagged.
 *
 * @param tag_type	The type of the implicit tag.
 * @param tag	The implicit tag.
 * @return	The BER encoding of the object.
 * @exception	ASN1Exception When invalid or cannot be encoded.
 * @see asn1.BEREncoding#UNIVERSAL_TAG
 * @see asn1.BEREncoding#APPLICATION_TAG
 * @see asn1.BEREncoding#CONTEXT_SPECIFIC_TAG
 * @see asn1.BEREncoding#PRIVATE_TAG
 */

public BEREncoding
ber_encode(int tag_type, int tag)
       throws ASN1Exception
{
  // Calculate the number of fields in the encoding

  int num_fields = 2; // number of mandatories
  if (s_tagType != null)
    num_fields++;
  if (s_tagOccurrence != null)
    num_fields++;
  if (s_metaData != null)
    num_fields++;
  if (s_appliedVariant != null)
    num_fields++;

  // Encode it

  BEREncoding fields[] = new BEREncoding[num_fields];
  int x = 0;
  BEREncoding enc[];

  // Encoding s_tagType: INTEGER OPTIONAL

  if (s_tagType != null) {
    fields[x++] = s_tagType.ber_encode(BEREncoding.CONTEXT_SPECIFIC_TAG, 1);
  }

  // Encoding s_tagValue: StringOrNumeric 

  enc = new BEREncoding[1];
  enc[0] = s_tagValue.ber_encode();
  fields[x++] = new BERConstructed(BEREncoding.CONTEXT_SPECIFIC_TAG, 2, enc);

  // Encoding s_tagOccurrence: INTEGER OPTIONAL

  if (s_tagOccurrence != null) {
    fields[x++] = s_tagOccurrence.ber_encode(BEREncoding.CONTEXT_SPECIFIC_TAG, 3);
  }

  // Encoding s_content: ElementData 

  enc = new BEREncoding[1];
  enc[0] = s_content.ber_encode();
  fields[x++] = new BERConstructed(BEREncoding.CONTEXT_SPECIFIC_TAG, 4, enc);

  // Encoding s_metaData: ElementMetaData OPTIONAL

  if (s_metaData != null) {
    fields[x++] = s_metaData.ber_encode(BEREncoding.CONTEXT_SPECIFIC_TAG, 5);
  }

  // Encoding s_appliedVariant: Variant OPTIONAL

  if (s_appliedVariant != null) {
    fields[x++] = s_appliedVariant.ber_encode(BEREncoding.CONTEXT_SPECIFIC_TAG, 6);
  }

  return new BERConstructed(tag_type, tag, fields);
}

//----------------------------------------------------------------
/**
 * Returns a new String object containing a text representing
 * of the TaggedElement. 
 */

public String
toString()
{
  StringBuffer str = new StringBuffer("{");
  int outputted = 0;

  if (s_tagType != null) {
    str.append("tagType ");
    str.append(s_tagType);
    outputted++;
  }

  if (0 < outputted)
    str.append(", ");
  str.append("tagValue ");
  str.append(s_tagValue);
  outputted++;

  if (s_tagOccurrence != null) {
    if (0 < outputted)
    str.append(", ");
    str.append("tagOccurrence ");
    str.append(s_tagOccurrence);
    outputted++;
  }

  if (0 < outputted)
    str.append(", ");
  str.append("content ");
  str.append(s_content);
  outputted++;

  if (s_metaData != null) {
    if (0 < outputted)
    str.append(", ");
    str.append("metaData ");
    str.append(s_metaData);
    outputted++;
  }

  if (s_appliedVariant != null) {
    if (0 < outputted)
    str.append(", ");
    str.append("appliedVariant ");
    str.append(s_appliedVariant);
    outputted++;
  }

  str.append("}");

  return str.toString();
}

//----------------------------------------------------------------
/*
 * Internal variables for class.
 */

public ASN1Integer s_tagType; // optional
public StringOrNumeric s_tagValue;
public ASN1Integer s_tagOccurrence; // optional
public ElementData s_content;
public ElementMetaData s_metaData; // optional
public Variant s_appliedVariant; // optional

} // TaggedElement

//----------------------------------------------------------------
//EOF
