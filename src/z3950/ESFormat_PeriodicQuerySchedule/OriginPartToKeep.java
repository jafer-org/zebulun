/*
 * $Source$
 * $Date$
 * $Revision$
 *
 * Copyright (C) 1998, Hoylen Sue.  All Rights Reserved.
 * <h.sue@ieee.org>
 *
 * This program is distributed in the hope that it will be useful,
 * but WITHOUT ANY WARRANTY; without even the implied warranty of
 * MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  Refer to
 * the supplied license for more details.
 *
 * Generated by Zebulun ASN1tojava: 1998-09-08 03:15:26 UTC
 */

//----------------------------------------------------------------

package z3950.ESFormat_PeriodicQuerySchedule;
import asn1.*;
import z3950.ESFormat_ExportSpec.Destination;
import z3950.ESFormat_ExportSpec.ExportSpecification;
import z3950.v3.IntUnit;
import z3950.v3.InternationalString;
import z3950.v3.Query;

//================================================================
/**
 * Class for representing a <code>OriginPartToKeep</code> from <code>ESFormat-PeriodicQuerySchedule</code>
 *
 * <pre>
 * OriginPartToKeep ::=
 * SEQUENCE {
 *   activeFlag [1] IMPLICIT BOOLEAN
 *   databaseNames [2] IMPLICIT SEQUENCE OF InternationalString OPTIONAL
 *   resultSetDisposition [3] IMPLICIT INTEGER OPTIONAL
 *   alertDestination [4] EXPLICIT Destination OPTIONAL
 *   exportParameters [5] EXPLICIT OriginPartToKeep_exportParameters OPTIONAL
 * }
 * </pre>
 *
 * @version	$Release$ $Date$
 */

//----------------------------------------------------------------

public final class OriginPartToKeep extends ASN1Any
{

  public final static String VERSION = "Copyright (C) Hoylen Sue, 1998. 199809080315Z";

//----------------------------------------------------------------
/**
 * Default constructor for a OriginPartToKeep.
 */

public
OriginPartToKeep()
{
}

//----------------------------------------------------------------
/**
 * Constructor for a OriginPartToKeep from a BER encoding.
 * <p>
 *
 * @param ber the BER encoding.
 * @param check_tag will check tag if true, use false
 *         if the BER has been implicitly tagged. You should
 *         usually be passing true.
 * @exception	ASN1Exception if the BER encoding is bad.
 */

public
OriginPartToKeep(BEREncoding ber, boolean check_tag)
       throws ASN1Exception
{
  super(ber, check_tag);
}

//----------------------------------------------------------------
/**
 * Initializing object from a BER encoding.
 * This method is for internal use only. You should use
 * the constructor that takes a BEREncoding.
 *
 * @param ber the BER to decode.
 * @param check_tag if the tag should be checked.
 * @exception ASN1Exception if the BER encoding is bad.
 */

public void
ber_decode(BEREncoding ber, boolean check_tag)
       throws ASN1Exception
{
  // OriginPartToKeep should be encoded by a constructed BER

  BERConstructed ber_cons;
  try {
    ber_cons = (BERConstructed) ber;
  } catch (ClassCastException e) {
    throw new ASN1EncodingException
      ("Zebulun OriginPartToKeep: bad BER form\n");
  }

  // Prepare to decode the components

  int num_parts = ber_cons.number_components();
  int part = 0;
  BEREncoding p;
  BERConstructed tagged;

  // Decoding: activeFlag [1] IMPLICIT BOOLEAN

  if (num_parts <= part) {
    // End of record, but still more elements to get
    throw new ASN1Exception("Zebulun OriginPartToKeep: incomplete");
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() != 1 ||
      p.tag_type_get() != BEREncoding.CONTEXT_SPECIFIC_TAG)
    throw new ASN1EncodingException
      ("Zebulun OriginPartToKeep: bad tag in s_activeFlag\n");

  s_activeFlag = new ASN1Boolean(p, false);
  part++;

  // Remaining elements are optional, set variables
  // to null (not present) so can return at end of BER

  s_databaseNames = null;
  s_resultSetDisposition = null;
  s_alertDestination = null;
  s_exportParameters = null;

  // Decoding: databaseNames [2] IMPLICIT SEQUENCE OF InternationalString OPTIONAL

  if (num_parts <= part) {
    return; // no more data, but ok (rest is optional)
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() == 2 &&
      p.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    try {
      BERConstructed cons = (BERConstructed) p;
      int parts = cons.number_components();
      s_databaseNames = new InternationalString[parts];
      int n;
      for (n = 0; n < parts; n++) {
        s_databaseNames[n] = new InternationalString(cons.elementAt(n), true);
      }
    } catch (ClassCastException e) {
      throw new ASN1EncodingException("Bad BER");
    }
    part++;
  }

  // Decoding: resultSetDisposition [3] IMPLICIT INTEGER OPTIONAL

  if (num_parts <= part) {
    return; // no more data, but ok (rest is optional)
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() == 3 &&
      p.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    s_resultSetDisposition = new ASN1Integer(p, false);
    part++;
  }

  // Decoding: alertDestination [4] EXPLICIT Destination OPTIONAL

  if (num_parts <= part) {
    return; // no more data, but ok (rest is optional)
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() == 4 &&
      p.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    try {
      tagged = (BERConstructed) p;
    } catch (ClassCastException e) {
      throw new ASN1EncodingException
        ("Zebulun OriginPartToKeep: bad BER encoding: s_alertDestination tag bad\n");
    }
    if (tagged.number_components() != 1) {
      throw new ASN1EncodingException
        ("Zebulun OriginPartToKeep: bad BER encoding: s_alertDestination tag bad\n");
    }

    s_alertDestination = new Destination(tagged.elementAt(0), true);
    part++;
  }

  // Decoding: exportParameters [5] EXPLICIT OriginPartToKeep_exportParameters OPTIONAL

  if (num_parts <= part) {
    return; // no more data, but ok (rest is optional)
  }
  p = ber_cons.elementAt(part);

  if (p.tag_get() == 5 &&
      p.tag_type_get() == BEREncoding.CONTEXT_SPECIFIC_TAG) {
    try {
      tagged = (BERConstructed) p;
    } catch (ClassCastException e) {
      throw new ASN1EncodingException
        ("Zebulun OriginPartToKeep: bad BER encoding: s_exportParameters tag bad\n");
    }
    if (tagged.number_components() != 1) {
      throw new ASN1EncodingException
        ("Zebulun OriginPartToKeep: bad BER encoding: s_exportParameters tag bad\n");
    }

    s_exportParameters = new OriginPartToKeep_exportParameters(tagged.elementAt(0), true);
    part++;
  }

  // Should not be any more parts

  if (part < num_parts) {
    throw new ASN1Exception("Zebulun OriginPartToKeep: bad BER: extra data " + part + "/" + num_parts + " processed");
  }
}

//----------------------------------------------------------------
/**
 * Returns a BER encoding of the OriginPartToKeep.
 *
 * @exception	ASN1Exception Invalid or cannot be encoded.
 * @return	The BER encoding.
 */

public BEREncoding
ber_encode()
       throws ASN1Exception
{
  return ber_encode(BEREncoding.UNIVERSAL_TAG, ASN1Sequence.TAG);
}

//----------------------------------------------------------------
/**
 * Returns a BER encoding of OriginPartToKeep, implicitly tagged.
 *
 * @param tag_type	The type of the implicit tag.
 * @param tag	The implicit tag.
 * @return	The BER encoding of the object.
 * @exception	ASN1Exception When invalid or cannot be encoded.
 * @see asn1.BEREncoding#UNIVERSAL_TAG
 * @see asn1.BEREncoding#APPLICATION_TAG
 * @see asn1.BEREncoding#CONTEXT_SPECIFIC_TAG
 * @see asn1.BEREncoding#PRIVATE_TAG
 */

public BEREncoding
ber_encode(int tag_type, int tag)
       throws ASN1Exception
{
  // Calculate the number of fields in the encoding

  int num_fields = 1; // number of mandatories
  if (s_databaseNames != null)
    num_fields++;
  if (s_resultSetDisposition != null)
    num_fields++;
  if (s_alertDestination != null)
    num_fields++;
  if (s_exportParameters != null)
    num_fields++;

  // Encode it

  BEREncoding fields[] = new BEREncoding[num_fields];
  int x = 0;
  BEREncoding f2[];
  int p;
  BEREncoding enc[];

  // Encoding s_activeFlag: BOOLEAN 

  fields[x++] = s_activeFlag.ber_encode(BEREncoding.CONTEXT_SPECIFIC_TAG, 1);

  // Encoding s_databaseNames: SEQUENCE OF OPTIONAL

  if (s_databaseNames != null) {
    f2 = new BEREncoding[s_databaseNames.length];

    for (p = 0; p < s_databaseNames.length; p++) {
      f2[p] = s_databaseNames[p].ber_encode();
    }

    fields[x++] = new BERConstructed(BEREncoding.CONTEXT_SPECIFIC_TAG, 2, f2);
  }

  // Encoding s_resultSetDisposition: INTEGER OPTIONAL

  if (s_resultSetDisposition != null) {
    fields[x++] = s_resultSetDisposition.ber_encode(BEREncoding.CONTEXT_SPECIFIC_TAG, 3);
  }

  // Encoding s_alertDestination: Destination OPTIONAL

  if (s_alertDestination != null) {
    enc = new BEREncoding[1];
    enc[0] = s_alertDestination.ber_encode();
    fields[x++] = new BERConstructed(BEREncoding.CONTEXT_SPECIFIC_TAG, 4, enc);
  }

  // Encoding s_exportParameters: OriginPartToKeep_exportParameters OPTIONAL

  if (s_exportParameters != null) {
    enc = new BEREncoding[1];
    enc[0] = s_exportParameters.ber_encode();
    fields[x++] = new BERConstructed(BEREncoding.CONTEXT_SPECIFIC_TAG, 5, enc);
  }

  return new BERConstructed(tag_type, tag, fields);
}

//----------------------------------------------------------------
/**
 * Returns a new String object containing a text representing
 * of the OriginPartToKeep. 
 */

public String
toString()
{
  int p;
  StringBuffer str = new StringBuffer("{");
  int outputted = 0;

  str.append("activeFlag ");
  str.append(s_activeFlag);
  outputted++;

  if (s_databaseNames != null) {
    if (0 < outputted)
    str.append(", ");
    str.append("databaseNames ");
    str.append("{");
    for (p = 0; p < s_databaseNames.length; p++) {
      if (p != 0)
        str.append(", ");
      str.append(s_databaseNames[p]);
    }
    str.append("}");
    outputted++;
  }

  if (s_resultSetDisposition != null) {
    if (0 < outputted)
    str.append(", ");
    str.append("resultSetDisposition ");
    str.append(s_resultSetDisposition);
    outputted++;
  }

  if (s_alertDestination != null) {
    if (0 < outputted)
    str.append(", ");
    str.append("alertDestination ");
    str.append(s_alertDestination);
    outputted++;
  }

  if (s_exportParameters != null) {
    if (0 < outputted)
    str.append(", ");
    str.append("exportParameters ");
    str.append(s_exportParameters);
    outputted++;
  }

  str.append("}");

  return str.toString();
}

//----------------------------------------------------------------
/*
 * Internal variables for class.
 */

public ASN1Boolean s_activeFlag;
public InternationalString s_databaseNames[]; // optional
public ASN1Integer s_resultSetDisposition; // optional
public Destination s_alertDestination; // optional
public OriginPartToKeep_exportParameters s_exportParameters; // optional

//----------------------------------------------------------------
/*
 * Enumerated constants for class.
 */

// Enumerated constants for resultSetDisposition
public static final int E_replace = 1;
public static final int E_append = 2;
public static final int E_createNew = 3;

} // OriginPartToKeep

//----------------------------------------------------------------
//EOF
